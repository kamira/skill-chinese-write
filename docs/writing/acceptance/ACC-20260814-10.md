# ACC-20260814-10 — 驗收:退役六個 `fiction-*` plugin(CHG-20260814-10)

- 驗收者:Claude Opus 5(Cowork session)
- 審議席:**Claude Fable 5**(設計 + 實作覆核,九發 probe + 突變測試)
  / **OpenAI Codex**(連線逾時,**未審**)
- 風險分級:高(移除已發布 plugin id,不可逆)
- 分支:claude/fiction-retire
- 日期:2026-08-14(UTC+0)
- 對應變更:CHG-20260814-10

## 查核明細

| # | 條件 | 結果 | 證據 |
|---|---|---|---|
| 1 | `RETIRED` 名冊六支具名 | ✅ | 證據 A |
| 2 | R5/R6/R7 紅綠端;**R5 紅端在真實退役上驗過** | ✅ | 證據 B |
| 3 | `[G]` 相位切換兩案 | ✅ | 證據 C |
| 4 | **CJK 緊鄰兩案換宿主保留** | ✅ | 證據 C |
| 5 | 四份 CI 承重夾具搬家,三處引用同步 | ✅ | 證據 D |
| 6 | zh-style 去字面化;15 宿主全 bump | ✅ | 證據 E |
| 7 | 清點 plugin 21 → 15、skill 24 → 18 | ✅ | 證據 F |
| 8 | 四道專用閘 + 兩支 carrier 皆 exit 0 | ✅ | 證據 F |
| 9 | **commit 之後**再驗 `--since` | ✅ | 證據 B |
| 10 | 每個數字附產生它的指令 | ✅ | 本節即是 |

### 證據 A/B — 消失也看得見,紅端在真實退役上驗過

```
$ python - # 對真實 repo,把 RETIRED 視為空
現況:綠
RETIRED 視為空 → R5 抓到 6 個未具名的消失
    plugin「fiction-flash」自 e674a38 起從名冊消失,但沒有列進 RETIRED
    plugin「fiction-long」…
    plugin「fiction-mystery」…
```

**不是理論上會紅,是在這次真的退役上紅過。**

`--since` 是在 **commit 之後**跑的(`1bc3848`)——未 commit 的綠燈沒有意義,
那是 `CHG-20260814-06` 寫下、`CHG-20260814-09` 我又踩過一次的教訓。

### 證據 C — 複審的九發 probe 與突變測試

| probe | 結果 |
|---|---|
| marketplace entry 單獨復活 | 紅(R6) |
| 磁碟目錄復活(未追蹤即可) | 紅(R6) |
| `PLUGINS` 復活 + `RETIRED` 仍在 | 紅(R6) |
| 同 diff 復活 + 除名(繞過嘗試) | **紅(R7 擋住)** |
| 只刪 tombstone、不復活 | **綠——見「## 記帳不修」** |
| 舊住址 SKILL.md 復活 | 紅(`[G]`) |
| 半刪:目錄在、SKILL.md 不在 | 凍結閘盲,`skill_inventory_check` 紅 |
| 副本藏進 `plugins/fiction/skills/fiction-long/` | 兩閘盲,`skill_inventory_check` 紅 |

**突變測試證明案子不是名存實亡**:捕獲改回 `\S+` → 案 22 翻紅;
拔掉 `[G]` 整段 → 11/18/19 三案翻紅;分別拔 R5/R6/R7 → 34/36/37 各自翻紅。
**沒有恆真規則,沒有無紅端案。**

**CJK 緊鄰兩案是承重保住,不只名字保住**:案 22 在新宿主上,
把捕獲改回 `\S+` 立刻翻紅(實測捕成 `` long`),但區間是本 ``)。

### 證據 D/E/F — 搬家、去字面化、清點

```
$ grep -c "command_path_check\|sample-good-" .github/ci_local.sh   # 三處引用皆已改
$ python scripts/skill_inventory_check.py --repo .
skill 清點 — 18 支 skill / 15 個 plugin(磁碟上 15 個目錄)
✅ 一致。

版號:catalog 3.0.0 / fiction skill 1.2.0 / zh-style skill 1.1.0
      writing 1.5.0、fiction 1.3.0、其餘 13 支 1.1.0
$ bash .github/ci_local.sh    → exit=0
$ bash .github/verify.sh      → exit=0
$ version_impact_check --self-test   37 案
$ genre_ratio_freeze --self-test     25 案
```

## 複審打穿的兩處(都是我漏掉的一體適用)

### 一、帳本文件不在任何 commit 裡

`CHG-20260814-10.md` 寫在 commit **之後**,是未追蹤檔。
**分支 merge 進去,帳就丟了。**

### 二、「不改是明知的假話」我沒有一體適用

我把那個判準套在 zh-style,卻漏了三份**活文件**:

| 檔 | 假在哪 |
|---|---|
| `docs/genres.md` 32–37 | 前門表仍把六支退役的列成活 skill;43 行「每個前門的 plugin」對那六列已假 |
| `README.md` 34/35/48 | 「24 支 skill」「19 支前門」「21 個可安裝的 plugin」——現實是 18/15 |
| `docs/worklog/handshake-autopilot.md` 82 | 「本 repo **現在有** 24 支 skill / 21 個 plugin」——**現在式**,而 handshake 是每個新 session 進場要讀的 |

複審的原話:「CHG 自己寫『不改是明知的假話』;**這個判準要一體適用**。」

三份都不出貨,**零版號連鎖**,純文件修改。已改:
表格改指 `/fiction:fiction-<g>`,計數全部去字面化
(handshake 那句改成「確切數目以 `skill_inventory_check` 為準,寫死的計數會隨每次退役腐爛」)。

## 記帳不修(複審裁定非阻擋)

1. **`RETIRED` 名冊自身可被靜默縮水**——單獨刪一條 tombstone 全綠;
   **兩 PR 洗白路徑存在**(PR1 刪 tombstone 綠、PR2 復活只付正常 R2 bump,R7 不火)。
   **R7 的保證是 per-diff 的。** 可考慮反轉語意(「除名而不復活」才紅),
   交審議席合議,不入本張。
2. **R7 的復活綠端在單一 diff 內不可達**——除名就在同 diff 裡也照樣紅,
   訊息「那一步才看得見」言過其實。保守方向,可接受。
3. **R6 目錄檢查讀工作樹、其餘讀 blob**——本地可造出「HEAD 還載著檔、磁碟目錄已刪」
   的假綠,CI 全新 checkout 必紅。**失效方向安全**,註記即可。
4. `RETIRED` 可登記從未存在的 id 而全綠;freeze 自檢標號 16/17 重複;
   `[G]` 橫幅第三條其實不是 self-test 案。皆化妝品級。

## 我在本張裡犯的錯

1. **帳本寫在 commit 之後**(見上)。
2. **判準沒有一體適用**(見上)——這比第一條嚴重,因為它是**我自己在同一份 CHG 裡
   寫下的判準**,而我只套用在其中一個對象上。
3. **`git rm -r` 沒清掉目錄**——未追蹤的 `__pycache__` 留著,
   而**我新寫的 R6 正是查 `plugins/<p>/` 存不存在**。
   自己寫的斷言抓到自己的清理不完整。

## 未達標處

1. **codex 未審**——設計與實作覆核皆連線逾時,本張**單席成立**。
2. `RETIRED` 只擋名冊層的消失;半刪狀態由 `skill_inventory_check` 第 6/7 項擋,
   已寫進 docstring 免得兩閘互以為對方在管。

## 下一步(複審裁定的順序)

1. **`freeform` 大類(8 支)**——加法、低風險,`CHG-20260814-09` 的搬遷機具現成
2. **刪舊 plugin id**——減法、不可逆,吃本張剛建好的 `RETIRED` 機具

**「21 個」那條前置不回改 `CHG-20260814-05`**——歷史帳本不改寫。
那條的操作句是「精確列舉刪除目標,證明沒有第 N+1 個連帶」,數字只是當時快照;
新 CHG 開張時以自己的 base ref 重新量測列舉,並具名記差額(6 支已由本張退役)。

複審已用 probe 驗證 **R5/R6/R7 對 id 不可知**,下一批退役直接沿用,不需改閘。

## 結論

**通過(單席互審)。** 十條驗收條件全部以附指令的原始輸出滿足;
複審的兩條 merge-blocking 已修。

本張的價值在於它補上了一個**前五張都沒碰到的失效軸**:
**一起消失就是一致**——刪除這個動作,對每一道既有的閘都是不可見事件。
