# ACC-20260727-01 — 驗收 CHG-20260727-01(writing skill v1.0.0)

- 專案:ai-skills / skill: writing
- 分支(Branch):claude/writing-skill-article-review-92efbc
- 日期:2026-07-27(UTC+0)
- 標的:CHG-20260727-01
- 驗收者:Claude(同 session 自驗;低風險白名單——純加法、既有 skill 零變更、無下游語意改寫)
- 風險分級:低
- Commit/PR:`c5ecce0`(與 CHG 同 commit)| PR https://github.com/kamira/ai-skills/pull/15(merge commit `d478d54`)
- Skill: ai-sdlc v1.17 / ai-sdlc-autopilot(Acceptance operation 四步)
- 基準來源:CHG Tasks T1〜T10 + 使用者五條訴求 + `### Acceptance operation` 四步
- 結論:通過

## 查核明細

| # | 驗收條件 | 結果 | 證據(可重跑) |
|---|----------|------|----------------|
| 1 | 只做評論,小說/散文明確未實作 | 通過 | `skills/writing/SKILL.md` 文體表三列:評論=**可用**、小說=**未實作**、散文=**未實作**;並明文「不要拿評論的框架套上去假裝寫得了」,附三種文體骨架差異說明 |
| 2 | 訴求(3) 第一人稱可機器判定 | 通過 | `style_check.py` 全篇 `我(?!們)` 命中數=0 → 列硬性違規、exit 1;`sample-bad.md` 實測命中「(缺第一人稱)」。另有「我」首次出現位置門檻(200 字)與「我認為/我覺得」密度警告 |
| 3 | 訴求(4) 不強制前後句等長,對稱句只在零星重點 | 通過 | 落成兩組指標:節奏側 `rhythm.min_cv=0.35` + 連續 3 句差 ≤2 字判「平板連段」;對稱側 `symmetry.max_total=3` + `min_gap_sentences=3`(**配額而非禁止**,對應使用者「偶爾可以」)。good 樣稿實測 CV 0.611、對稱句 1 處 |
| 4 | 訴求(5) 禁「制度」「窗口」等過於 AI 的詞 | 通過 | 兩詞在 `style_rules.json` 的 hard_bans(group=管理黑話)並附替代寫法;`sample-bad.md` 實測命中 制度×3、窗口×1。hard 表共七群、含 regex 型(「隨著⋯⋯的發展」「不僅僅是⋯⋯更是」) |
| 5 | 訴求(1)(2) 不制式化、夠口語 | 通過(部分靠人判斷) | 可判定部分已機讀:膠水詞/開場罐頭/空心評價/動詞被架空/廢副詞五群 hard + soft 密度上限 + 條列佔比 15% + 破折號 4/千字 + 粗體一段一處。不可判定部分(角度是否新、講得夠不夠死)在 SKILL.md 末節明文交還給人 |
| 6 | Acceptance operation 步驟 1:good 樣稿 | 通過 | `style_check.py sample-good.md` → 硬性違規 0、提醒 0、exit 0;報表:420 字 / 22 句 / CV 0.611 / 對稱句 1 / 我×7 |
| 7 | Acceptance operation 步驟 2:bad 樣稿 | 通過 | 同一支腳本 → **硬性違規 40 處** + 缺第一人稱,exit 1;命中含 綜上所述、時代的浪潮、不僅僅是⋯⋯更是、筆者、本文、賦能、閉環、顆粒度、頂層設計 |
| 8 | Acceptance operation 步驟 3:`--allow` 逃生口 | 通過 | `--allow 制度,窗口` 重跑 bad 樣稿 → 該兩詞在輸出中命中數=0(`grep -c` 驗證),其餘違規不受影響 |
| 9 | Acceptance operation 步驟 4:同步與版本 | 通過 | `build_suite.py --check` exit 0(0 變更);`catalog_check.py --check` 與 `--since origin/main` 皆 exit 0;三份 JSON `json.tool` 全過;version 三處=1.0.0,catalog **1.6.2→1.7.0** |
| 9b | rebase 到 `main` 後複驗(基底已推進) | 通過 | rebase 衝突僅 `marketplace.json` 的 metadata 一段,取 main 基底 1.6.2 疊本次 minor → 1.7.0。重跑全套 CI 全綠,含 main 新增的 `build_dist.py --check`(dist 與源碼一致,4 包);`writing` 為繁中單語不入 dist,與 `intel-analysis` 一致 |
| 10 | 腳本錯誤路徑 | 通過 | 缺檔 → `ERROR: 找不到檔案` + exit 2;`--json` 輸出經 `json.tool` 驗證合法 |
| 11 | CI 迴歸閘門 | 通過 | 本地重跑 CI 六步全綠:py_compile(納入 writing)、JSON validity(納入 writing assets + plugin.json)、plan-check 迴圈(納入 docs/writing,本 CHG **plan-check 通過:10 個 task,格式完整**)、writing lint 夾具、build_suite --check、catalog_check |
| 12 | 既有 skill 零影響 | 通過 | `git status` 只有 7 個條目:4 個共用檔為機械式加法(marketplace entry+bump、build_suite 一列、CI 三處納入+一步新增、README 一列+一行),3 個為新目錄(docs/writing、plugins/writing、skills/writing);`skills/ai-sdlc*`、`skills/intel-analysis` 零變更 |

## 未達標項與處置

無阻斷項。三項已知限制,已寫進 CHANGELOG 的「已知限制」節,不視為未達標:

1. **只做評論**。小說、散文未實作——這是使用者本輪明示的範圍,不是缺口。
2. **繁中先行,英文版列 backlog**。陸味對照與中文句長節奏本質綁定中文,英文需另立規則集(對齊 intel-analysis 慣例)。
3. **lint 擋不住「無聊」**。它只保證稿子不像機器寫的,不保證觀點值得讀;SKILL.md 末節明文交還給人。

跨帳本待辦(不在本 CHG 範圍,已於 CHG 重複性檢查欄註記):`docs/intel-analysis/knowledge` KN-001「新能力域以 skill+帳本+lint+plugin 四件套落地」計數器應為 seen 2→3 / applied 1→2,留待 intel-analysis 下次 CHG 回填。

## 結構一致性檢查

- [x] 規則正典=`skills/writing/assets/style_rules.json`;references 為人類可讀版。此分工在 SKILL.md、voice.md、ai-tells.md 三處抬頭明文,且三處都寫「衝突以 JSON 為準」
- [x] 單一真相在 `skills/`,plugin 內為 build_suite 產生的複本(8 檔),`--check` 驗同步
- [x] 版本三處同步(SKILL.md metadata / plugin.json / marketplace entry = 1.0.0),catalog metadata.version 隨 plugin 變動 bump 至 1.7.0
- [x] 帳本落在 `docs/writing/`(本 repo 慣例,非預設 `docs/`),無平行帳本
- [x] 文件示範自己的規則:三份 reference 與 SKILL.md 皆第一人稱口語、句長參差、未使用自家 hard 表詞彙(自家 hard 詞僅以「反例/清單項」形式出現)

## 總結

可交付。使用者五條訴求全數落地,其中四條有機器斷言:第一人稱(缺即 exit 1)、句長參差(CV 門檻 + 平板連段偵測)、對稱句零星使用(**配額 3 處、需隔 3 句**,不是禁止)、禁 AI 腔與公文腔(七群 hard 表 + `--allow` 逃生口 + 陸味 24 組對照)。第五條「不制式化」拆成可判定與不可判定兩半,可判定的入詞表與排版門檻,不可判定的在 SKILL.md 明講「lint 過了不等於文章好」。

依 `docs/ai-sdlc/knowledge` KN-001,風格規則若只寫成散文不會被執行——本 skill 因此把門檻全部寫進 `style_rules.json`,並用一好一壞兩份樣稿當 CI 夾具:good 必過、bad 必擋,詞表或門檻被改壞時 CI 會紅。
