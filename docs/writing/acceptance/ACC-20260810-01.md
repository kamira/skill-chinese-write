# ACC-20260810-01 — 驗收:repo 改名 + 治理閘本機載體(CHG-20260810-02)

- 驗收者:Claude(Cowork session;與實作者同一個——中風險原則上要獨立驗收,本輪未具備獨立驗收者,見「誠實的邊界」)
- 風險分級:中
- 分支:claude/ai-sdlc-autopilot-handshake-4a3f7a
- 日期:2026-08-10(UTC+0)
- 對應變更:CHG-20260810-02(同 PR 另含 CHG-20260810-01)

## 查核明細

| # | 驗收條件(對應使用者故事) | 結果 | 證據 |
|---|---------------------------|------|------|
| 1 | repo 改名生效(故事 1) | 通過 | `gh repo view kamira/skill-chinese-write` → `{"name":"skill-chinese-write"}` |
| 2 | 本機 remote 指向新 URL | 通過 | `git remote -v` → `https://github.com/kamira/skill-chinese-write.git`;`git fetch origin` 成功 |
| 3 | repo 內引用全數對齊 | 通過 | `marketplace.json` name、README 標題與安裝指令、DIR-001、CHG-20260810-01 的 PR 連結;舊 CHG 的 `Project:` 依決策**刻意保留** |
| 4 | 改名這件事對安裝者可見(故事 2) | 通過 | README 安裝節寫明舊名、轉址、以及 marketplace id 要移除再重加 |
| 5 | 本機治理閘綠燈可達(故事 3) | 通過 | `bash .github/ci_local.sh` exit **0**,五步全過 |
| 6 | 本機閘含**紅燈可達**探針 | 通過 | 第 1 步呼叫 `verify.sh`,其第 4 步刻意改副本使漂移檢查轉紅,不紅即 exit 1;還原後重驗回綠 |
| 7 | pre-push hook 真的裝上 | 通過 | `git config core.hooksPath` → `.githooks`;本次 push 由 hook 實跑一次(見下) |
| 8 | catalog 不需 bump | 通過 | `plugins/` 與 `skills/` 零改動,`catalog_check --since origin/main` exit 0 |
| 9 | 公開前內容普查(故事 4) | 通過 | 見下表 |
| 10 | 轉 public 停在停點 | 通過 | 步驟 6、7 未打勾;`gh repo view` 仍為 `PRIVATE` |

## 公開前普查(這是「可不可以轉 public」的證據,不是形式)

| 查核 | 結果 |
|------|------|
| 全歷史憑證掃描(`git log -p --all`,比對 ghp_ / github_pat_ / sk- / AKIA / PRIVATE KEY / password= / secret=) | **零命中** |
| 工作區憑證與信箱掃描 | 唯二命中是 `doc_integrity_check.py` 與 `static_check.py` **偵測器自身的 regex**,不是憑證 |
| `__pycache__` / `*.pyc` 是否入版控 | **未追蹤**,且已列於 `.gitignore` |
| 追蹤檔總數 | 97 |
| 授權 | MIT(`LICENSE` 存在) |

## 這一輪最值得記的一件事

使用者最初的指示是「不需要 full-ci」,照字面做就是去砍 workflow 的步驟。**那會是個無效解**:
私有 repo 的 Actions 被帳務擋住時,擋的是 job 的**啟動**,不是某一步;workflow 砍到剩一行照樣紅。

差別在於有沒有去讀那行錯誤訊息。`The job was not started because recent account payments have failed`
這句話裡沒有任何一個字提到步驟。照字面執行會產生一個看起來有做事、實際上 PR 依然卡住的結果——
而且還順手拆掉了一道原本好好的閘。

## 誠實的邊界

1. **驗收者與實作者是同一個。** 中風險原則上要獨立驗收(verifier ≠ implementer),本輪 session
   沒有第二個驗收者,ACC 如實記下,不假裝滿足。
2. **hook 沒辦法自動生效。** `core.hooksPath` 不隨 clone 帶過來,沒跑過 `scripts/install-hooks.sh`
   的機器 push 時沒有任何閘。真正擋在 merge 前的是 autopilot runner 的 local-gate。
3. **轉 public 未驗證**,因為未執行。步驟 6、7 是停點,不是遺漏。

## 結論

**通過(範圍限步驟 1~5)。** 改名生效、引用對齊、本機閘綠燈與紅燈皆可達、公開前普查零命中。
步驟 6(轉 public)與步驟 7(merge)停在停點等使用者確認,不列入本次通過範圍。
