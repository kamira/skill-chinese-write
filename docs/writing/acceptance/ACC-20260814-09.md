# ACC-20260814-09 — 驗收:批量搬遷五支流派(CHG-20260814-09)

- 驗收者:Claude Opus 5(Cowork session)
- 審議席:**Claude Fable 5** + **OpenAI Codex**
- 風險分級:高
- 分支:claude/fiction-batch
- 日期:2026-08-14(UTC+0)
- 對應變更:CHG-20260814-09

## 查核明細

| # | 條件 | 結果 | 證據 |
|---|---|---|---|
| 1 | 六支 command 齊備,`sync_commands` 雙向綠 | ✅ | 證據 A |
| 2 | 路徑閘綠,逐支對帳 | ✅ | 證據 B |
| 3 | 凍結閘綠 = 配比未漂的機器證明 | ✅ | 證據 C |
| 4 | `[E]` 紅綠端五案;逐支 `--genre` 對帳 | ✅ | 證據 D |
| 5 | 誠實段模板一致性 | ✅ | 證據 E |
| 6 | 五份 description 逐支眼讀 | ✅ | 證據 F |
| 7 | bump 在 commit 1;**每個 commit 單獨跑閘皆綠** | ✅ | 證據 G |
| 8 | catalog bump;兩支 carrier exit 0 | ✅ | 證據 H |
| 9 | 每個數字附產生它的指令 | ✅ | 本節即是 |

### 證據 A — 同步

```
$ python plugins/build_suite.py --check
[check] 新增 0 / 更新 0 / 移除 0(共 0 變更)
exit=0
```

五個 commit **每一步**都跑過 `--check` 且綠(見證據 G 的說明)。

### 證據 B — 路徑閘與逐支對帳

```
$ python scripts/command_path_check.py --repo .
✅ command 引用路徑閘:6 組 (plugin, 命令) × 2 個住址;
抽到連結 12 條、程式碼路徑 6 條(頂層實體計)
exit=0
```

**逐支對帳(目的是查多餘與錯置,不是抄數字)**:
六支各 2 條連結(`references/dialogue.md`、`references/structure.md`)+
1 條程式碼路徑(`skills/fiction/scripts/fiction_check.py`)= 12 / 6,
與檔面一致,無多餘、無錯置。

### 證據 C — 凍結閘(**不手抄數字**)

```
$ python scripts/genre_ratio_freeze.py --repo .
✅ 配比凍結閘:6 支流派 × 三欄與 ref 6fea545 一致;誠實欄驗到 12 個 command 檔
exit=0
```

審議席(codex)裁定:**凍結閘 `[B]` 全綠即構成「配比字面未漂」的證明**,
ACC 引用閘的輸出行,**不再手抄一份數字**——手抄是另造一份會漂的第二真相,
而這個 repo 已經為「用回憶代替量測」付過四次代價。

fable 的 caveat 一併記:`[B]` 的判準是「找到的區間集合**包含**凍結值」,
不是「只含」。所以逐支人工對帳仍要做,**目的是查多餘與錯置**(見證據 B)。

### 證據 D — `[E]` genre token

```
$ python scripts/genre_ratio_freeze.py --self-test | head -1
✅ self-test:22 案全過(19 個文案案 + [D] 執法探針 3 案)……
[E] genre token 五條(command 錯置 / command 缺 token /
舊 SKILL.md 錯置也紅 / 舊 SKILL.md 缺 token 豁免 / 正確 token 在場也擋不住旁邊錯貼的)

$ 逐支對帳
  fiction-flash.md    genre=['flash']    OK
  fiction-long.md     genre=['long']     OK
  fiction-mystery.md  genre=['mystery']  OK
  fiction-romance.md  genre=['romance']  OK
  fiction-scifi.md    genre=['scifi']    OK
  fiction-wuxia.md    genre=['wuxia']    OK
```

### 證據 E — 誠實段模板一致性

槽位(`<PCT>` / `<RANGE>` / `--genre <G>` / `上限 <HI>`)正規化後:

```
正規化後的唯一字串數 = 1
變體 1: 6 支 [flash, long, mystery, romance, scifi, wuxia]
```

審議席否決了「把措辭凍進 regex」——那會把閘變成它自己拒絕成為的東西
(它的註解說「那會變成關鍵字遊戲」)。統一措辭改由**模板化生成 + 一次性
機器比對**達成,不入 repo、不成為常設閘。

### 證據 F — description 逐支眼讀

審議席裁定**不機器化**:錨定詞表是**代理指標,不是不變量本身**
——「description 描述的是這個流派」機器判不了,而詞表會凍住措辭
(有人把「微型小說」合法改寫成「極短篇」就誤紅),巧妙的錯貼又照樣綠。

六支逐支讀過,各自描述的是自己的流派,無張冠李戴。

### 證據 G — 每個 commit 單獨跑閘

```
分岔點 = 0d8e6ee,其後 6 個 commit,逐一單獨判定
  1. a0cb186 綠   (前置:[E] 斷言)
  2. 154a3e8 綠   (1/5 flash + bump)
  3. e7c40ee 綠   (2/5 mystery)
  4. d53c461 綠   (3/5 romance)
  5. 182e07a 綠   (4/5 scifi)
  6. c051946 綠   (5/5 wuxia)
```

**做法本身也是證據**:這一輪不用 `git checkout` 逐個切,而是把每個 commit
當 `head` 參數傳給 `check()`——兩端本來就都吃 committed 狀態,工作樹全程不動。
第一版用 checkout 的做法逾時被殺、`finally` 沒跑到,**把 HEAD 留在 detached**
(見下「我犯的錯」)。

### 證據 H — 版號與 carrier

```
entry / plugin.json      1.1.0 → 1.2.0   ← plugin 真的多了五個 command
skills/fiction/SKILL.md  維持 1.1.0      ← fiction skill 內容一個字沒改
catalog                  2.5.0 → 2.6.0

$ bash .github/ci_local.sh   → exit=0
$ bash .github/verify.sh     → exit=0
$ python tools/autopilot/scripts/doc_integrity_check.py --repo . → exit=0
$ python plugins/catalog_check.py --repo . --since origin/main   → exit=0
```

## 我在本張裡犯的三個錯,全部由既有的閘抓到

**這是這批閘第一次在我做事的過程中發揮作用,而不是在覆核時。**

### 一、戳記凍結擋下我自己(`version_impact_check`)

我照 `CHG-20260814-05` 的老樣子,bump 時把 `skills/fiction/SKILL.md` 的版號
也動了。閘紅得精確:

```
skill「fiction」的內容自 0d8e6ee 起一個 byte 都沒變,
但 metadata.version 從 1.1.0 動到 1.2.0——戳記凍結
```

**`CHG-20260814-06` 已經退役三處同步斷言**——skill 版號本來就不該跟著 plugin 走。
我用的是被退役的那個習慣。

**而當時 `--since origin/main` 是綠的**,因為改動還沒 commit、`base == head`。
那正是我自己在 `CHG-20260814-06` 寫下的那句話:
**「本地 `--since` 在 commit 前的綠燈沒有意義。」**
我寫下它,然後走進去了。

### 二、regex 打中錯的名冊(`build_suite --check`)

`PLUGINS` 與 `COMMANDS` **兩張名冊都有 `"fiction": (...)`**,而我的替換沒錨定區塊,
打中了先出現的 `PLUGINS`,把它覆寫成 command 檔名清單。

```
ERROR: 缺來源 skills/fiction-long.md
```

修法不只是錨定區塊,還加了**每次替換後驗 `PLUGINS` 沒被動到**的自我檢查
——那是上一輪缺的那道。

### 三、反向斷言擋下中途狀態(`sync_commands`)

五支檔全在磁碟上、名冊只宣告到第二支:

```
頂層 commands/ 有沒被任何 plugin 宣告的命令:
fiction-mystery.md、fiction-romance.md、fiction-scifi.md、fiction-wuxia.md
```

**閘是對的。** 真正的循序工作流裡,每支檔是在它自己那一步才產生的;
我這邊五支已全在磁碟上,所以改成**逐步從暫存區放入**,每步 `--check` 綠才 commit。

### 附帶:第二次踩「演練沒還原」

逐 commit 驗證的第一版用 `git checkout` 逐個切,逾時被殺,`finally` 沒跑到,
**HEAD 被留在 detached(`9f5f244`)**。已還原,並改成不 checkout 的做法。

`ACC-20260814-06` 記過同一件事(cp950 的 print 炸掉,`write_text` 在它後面)。
**兩次的形狀相同:清理寫在會失敗的東西後面。**

## 審議席抓到而我沒抓到的

1. **`--genre` 交叉錯置**(fable 提出、codex 裁定成常設閘)——三道閘全綠的洞,
   而 lint 呼叫行是獨立的一行,**批量複製貼上最容易錯在這**。
2. **commit 粒度的前提是我餵錯的**(fable 糾正)——我以為「五個 commit 要 bump 五次」,
   而 `resolve_base()` 取的是 merge-base,兩點比較。codex 的第一次裁決
   **建立在我給的假前提上**,收到更正後改判,兩席一致。
3. **五支必須登記 `COMMANDS["fiction"]`**(fable)——凍結閘的第三住址**寫死**
   `plugins/fiction/commands/`,登記到各流派 plugin 會讓副本落在別處而
   **凍結閘靜默不驗**,正是它自己註解說的「只驗其中一處,另外兩處就是下一個孤兒」。
4. **「ACC 不是契約」**(fable 推翻自己前一輪)——它不會再跑第二次,
   所以當修補成本是既有閘裡加一條斷言時,具名風險卻拒絕一行修補,是倒錯的。

## 我在做 `[E]` 斷言時犯的錯:夾具字元集的盲區

`--genre` 的捕獲初版寫 `\S+`。**CJK 之間沒有空格**,於是舊 SKILL.md 的
``lint 會數(`--genre flash`),但區間是本 skill`` 被捕成 `` flash`),但區間是本 ``,
**在一份完全正確的檔案上假紅**。

「規則對正確輸入恆假」的第三次,而且**是真實 repo 跑出來才發現的**
——合成夾具全部用純 ASCII,那個環境永遠照不到。

這揭示的不只是一個 bug:**夾具的字元集也是夾具的一部分**。
中文專案的閘,自檢裡沒有中文,就等於沒驗過真實輸入。
**這是所有現存閘的共同盲區**,列入殘留風險。

## 未達標處

1. **兩席尚未覆核本張的實作**(設計已裁決)。
2. **本批不含退役**——六個 `fiction-*` plugin 與六份舊 SKILL.md 的去留,
   需要「plugin 整個消失也看得見」的閘,批量後另立 CHG,已在 CHG 具名為 next step。
3. 殘留風險三項見 CHG:舊檔的半真文案(死期具名)、夾具字元集盲區、
   `CHG-20260814-08` 的六種契約外形狀。

## 結論

**通過(自驗)。** 九條驗收條件全部以附指令的原始輸出滿足。

本張的價值不在搬了五支 skill,在於**這批閘第一次在施工過程中擋下了施工者**
——三個錯各自被一道閘接住,而那三個都不是程式邏輯寫錯,是**操作與規則不一致**。
