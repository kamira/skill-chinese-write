# ACC-20260822-01 — 「施工中必紅」與「CI 當作 commit 判準」拆層(BL-030)

- Project: skill-chinese-write
- Date: 2026-08-22 (UTC+0)
- Branch: claude/bl-030-two-tier
- 對應 CHG: `CHG-20260822-01`
- 審議席: **OpenAI Codex**、**Claude Fable 5** —— 三輪,三項全部兩席明示同意同一處置版本
- 結論: **通過(兩席互審共識)**。`BL-030` 解除。

## 逐項承接

| 修正項目 ID | 處置 | 共識狀態 | 輪次 | 驗收結果 |
|---|---|---|---|---|
| CHG-20260822-01.1 | 判定二拆成兩個具名紅出口 + `--allow-wip` + self-test | 通過 | 3 | 通過 |
| CHG-20260822-01.2 | `ci_local.sh` 只認 argv;`carrier_manifest_check` 結構釘死 | 通過 | 3 | 通過 |
| CHG-20260822-01.3 | `BL-030` 解除 + 誠實極限入 `CARRIER_ENV` 註解 | 通過 | 3 | 通過 |

## 病灶:閘執行的是不變量的過近似

要守的不變量是「Status 說施工中的 CHG **不得進 main**」;
閘實際執行的是「**任何時候**都不得有 CHG 說施工中」。後者嚴格強於前者,
強出來的部分正好覆蓋**治理流程明文要求存在的合法施工期**。

fable 再切一刀,而這一刀決定了選型:被吞掉的不是一條,是**兩條不同的不變量被同一個紅出口混判**——
(a) 施工中不得存在於 main;(b) Status 不得與**已存在的 ACC** 自相矛盾。

## 驗收條件與證據

### 1 兩個具名紅出口,四種組合全測

| 條件 | 結果 |
|---|---|
| 合法施工期(施工中 ∧ 無 ACC)+ 無旗標 | 紅 `RED(wip-not-explicitly-allowed)` |
| 合法施工期 + `--allow-wip` | 綠 |
| ACC 已存在 ∧ Status 仍施工中 + 無旗標 | 紅 `RED(acc-contradiction)` |
| **ACC 已存在 + 帶旗標** | **仍紅** |
| 已收尾 ∧ ACC 存在 | 綠 |

**第四格是本案的重點**:旗標放不了 `632cbe4` 那一型(ACC 寫完了、Status 忘了改)。
另加一端:**合併驗收的 ACC 也要認得**——`CHG-…-03/-04/-05` 共用 `ACC-…-02` 是本帳本的真實形狀,
只比對同尾碼會漏掉,矛盾態就被誤放行。

### 2 旗標為什麼走命令列而不是環境變數

第一版設計是環境變數 `CI_ALLOW_WIP`,理由是「取 `CI_` 前綴才落在 `ENV_TOKEN` 的掃描域內,
守衛才看得見」。**fable 第三輪自己推翻了它**:

> **環境是可寫的通道。** GitHub Actions 的 `GITHUB_ENV` 允許前置步驟
> `echo "NAME=value" >> "$GITHUB_ENV"` 注入,而那行字串**可以拼接構造**——
> 變數名七個字元不必完整出現在 workflow 檔的任何地方,文字層掃描**必然全盲**。
> 我把一道已知繞得過的文字斷言,放在了唯一守衛的位置。

以及一個機械事實,是這個選擇的加分:

> **argv 穿不過 `git push`。** pre-push hook 的呼叫形式固定,沒辦法對一次 push 臨時傳參數——
> 於是 hook **恆為全嚴**,施工中的東西一個位元組都出不了本機。
> `CI_ALLOW_WIP=1 git push` 則會穿過去。

兩席也一致否決了主 agent 提的第六案(拿 `CI_SINCE_REF` 或 merge base 當比較基準)。
fable 的理由最尖銳:`CI_SINCE_REF` 只在 push 事件被設定,拿「有設 / 沒設」切換嚴格度
**等於拿它當載體探針的代理**——「變數本身合法,但這種用法讓唯一真相源間接知道自己跑在哪個事件裡,
正是硬規矩禁止的事,只是換了件衣服。」

### 3 守衛:結構釘死,四端可達

`carrier_manifest_check.py` 新增 `APPROVED_CALL` 整段字面值比對,12 案全過:

| 端 | 判定 |
|---|---|
| g2 唯一真相源呼叫為核准字面值 | 綠 |
| **r3 workflow 帶 `--allow-wip`** | **紅**(本案的核心紅端) |
| r4 workflow 改呼叫別的入口 | 紅(不是只擋那一個字串) |
| r5 唯一真相源改成多行 `run:` 區塊 | 紅(**沒有東西守得住了 → fail-closed,不是靜默放行**) |

fable 指出這不需要真的「解析」指令列——抽出該步的 `run:` 純量與核准字串逐字比對即可,
也就不引入 `steps_of` 刻意迴避的 PyYAML 依賴。代價誠實寫在註解裡:
改用多行區塊寫同一道指令會紅,想改呼叫形式就得同步改核准值並在 CHG 說明。

### 4 釘子三:施工模式的綠與全嚴的綠長得不一樣

實跑 `bash .github/ci_local.sh --allow-wip`:

```
⚠️  施工模式(--allow-wip):判定二放行「施工中且對應 ACC 尚不存在」的 CHG。
    ACC 已存在而 Status 仍施工中,本參數不放行。正式 CI 不得帶這個參數。
⚠️  施工模式放行了 2 張:CHG-20260822-01.md、CHG-20260822-02.md
```

**逐張列出放行了誰。** 長期掛旗標不再是靜默綠——這是本 repo 對
「壞掉的東西輸出與真通過一字不差」的第四次教訓。

### 5 BL-030 用它自己當活體證明

同一棵樹,兩個問題,兩個不同的答案:

```
預設從嚴      → 紅(兩張在途 CHG 尚未收尾)  = 「收尾了沒」
帶 --allow-wip → 綠                          = 「東西壞了沒」
```

在此之前這兩個問題共用一個 rc。**這是 `BL-030` 三次現形以來第一次不用繞過。**

## 三輪收斂的路徑:兩席交叉改判

第二輪兩席都往對方那邊動,然後在半路交錯而過——fable 放棄 D+C 改採 codex 的 A 收緊版,
codex 同一輪放棄環境變數版改提命令列版。第三輪 fable 改判到命令列版,分歧歸零。

fable 界定自己第一輪論證的範圍,這一句值得留:

> 我證的是**僅憑 repo 狀態**,pre-push 與 PR CI 無法區分——這只否決了 C 單獨與維持現狀,
> **沒有否決引入明示意圖訊號**。

## 殘留風險(記帳不修)

- **誠實極限(兩席共記)**:「ci_local 不得因環境而變更判定二行為」對**任意命名**的環境變數
  沒有機器斷言(`ENV_TOKEN` 只掃三族,族外名字全盲)。這是既有的 `BL-017` 型極限,
  **不是本案新開的洞**——施工模式本身走命令列,不經環境。已記入 `CARRIER_ENV` 註解區。
- **本張自己是第四次繞過**:施工期間仍靠收尾後才跑全嚴閘。`.1` 落地之後,
  下一張 CHG 起才有真解。
- **`BL-006` 未解除**:驗收者與實作者仍是同一人;本張的補償是逐項寫明誰判的。

## 收尾覆驗

見 `## 收尾覆驗結果`。

## 收尾覆驗結果

`## Status` 回填之後,在**同一棵樹**跑**預設從嚴**(不帶 `--allow-wip`)的十九道閘:

```
bash .github/ci_local.sh
[1/19] … [18/19] plugin 隨附 skill 副本同步
✅ 治理閘通過(唯一真相源:.github/ci_local.sh)
CI_EXIT=0
```

**輸出裡沒有任何施工模式標示**——確認這一跑是全嚴的,不是靠旗標換來的綠。
第 [9/19] 步跑的是 `chg_field_check` 的四條判定(佔位字串 / Status 兩個具名紅出口 /
止血條款 / 審議席共識欄位)加份數硬條件;第 [15/19] 步新增
`ledger_coverage_honesty.py` 的自檢與實跑。`.github/workflows/governance.yml` 一行未動。

## Status

**已驗收 — `ACC-20260822-01`。** 三項皆為兩席互審共識,無單方、無僵局。
