# ACC-20260814-02 — 驗收:刪除兩個孤兒 plugin 目錄 + 反向斷言(CHG-20260814-02)

- 驗收者:Claude(Cowork session,Opus 5;與實作者同一個)
- 風險分級:中
- 分支:claude/zen-jepsen-2e1290
- 日期:2026-08-14(UTC+0)
- 對應變更:CHG-20260814-02

## 查核明細

| # | 驗收條件(對應使用者故事) | 結果 | 證據 |
|---|---------------------------|------|------|
| 1 | 新斷言對**真實輸入**紅燈可達(故事 2) | 通過 | **刪除前**跑 `skill_inventory_check.py --repo .`:exit 1,兩行訊息分別點名 `plugins/bizdoc/` 與 `plugins/techdoc/` 為孤兒目錄。這一步刻意排在刪除之前——只用合成樣本證明紅燈,等於沒證明它抓得到真的 |
| 2 | 兩個孤兒目錄刪除(故事 1、3) | 通過 | `git rm -r`:**23 個檔** deleted(bizdoc 12、techdoc 11)。另有 `git rm` 不會碰的未追蹤 `__pycache__` 殘留讓目錄還在,已一併 `rm -rf`——刪檔不等於刪目錄,是這輪實測抓到的 |
| 3 | 刪除後判定轉綠 | 通過 | `skill_inventory_check.py --repo .` exit 0,清點行由「磁碟上 23 個目錄」變為 **21 個**,與 21 個 plugin 相符 |
| 4 | self-test 綠紅皆可達(故事 2) | 通過 | 三向相等要過;三種偏離**各**要被抓:①孤兒目錄 ②名冊有磁碟沒有 ③兩份名冊自己分岔。只驗「相等會過」等於沒驗 |
| 5 | 未動到單一真相 | 通過 | `skills/bizdoc/` 與 `skills/techdoc/` 的 diff 為空;`build_suite.py --check` 回報「新增 0 / 更新 0 / 移除 0」——正當的建置產物(`plugins/official/skills/bizdoc/` 等)一個沒動 |
| 6 | 引擎仍裝得到、跑得動 | 通過 | 六份夾具判定不變:bizdoc gov/press good 皆 exit 0、techdoc spec/arch good 皆 exit 0;`ci_local.sh` 第 5、6 步(含四份 bad 夾具要被擋)全綠 |
| 7 | catalog 版本 bump | 通過 | marketplace `metadata.version` 2.2.0 → **2.2.1**;`catalog_check --check` 與 `--since origin/main` 皆 exit 0 |
| 8 | 13 步治理閘全綠 | 通過 | `bash .github/ci_local.sh` exit 0。基準線比對:**改動前也是全綠**——這正是問題所在,見下節 |
| 9 | KN-001 證據回填 | 通過 | 第九次觸發已寫入,計數 seen 9 / applied 9;INDEX 的一句規則由四個層次改為**五個**,新增「斷言的方向是不是只有單向」 |

## 這一輪最值得記的一件事

**改動前的 CI 也是全綠的。**

兩個目錄被 git 追蹤、與單一真相分岔 11 個檔、其中一份夾具用的是 zh-style 判為硬性違規的半形標點——而 13 道閘一道都沒響。原因不是任何一道閘壞掉,是它們**全部朝同一個方向走**:

| 閘 | 方向 |
|----|------|
| `catalog_check --check` | marketplace 名冊 → 磁碟 |
| `build_suite --check` | `PLUGINS` → plugin 內複本 |
| `skill_inventory_check` 第 2 項 | `PLUGINS` → `skills/` |

三道閘,三個起點,全部是**從名冊出發**。所以「沒被登記在名冊上」這件事本身,對它們是不可見的——不是判定錯,是根本走不到。

這與 CHG-20260810-07 的「規則管三處、斷言只查兩處」是同族,但更難察覺:那次至少有一份名冊列著第三處,對照就看得出來;這次**沒有任何一份文件說過 `plugins/` 底下應該有幾個目錄**,所以連可以對照的東西都不存在。

寫進 KN-001 的第五問是:凡是「A 的每一項都要在 B 裡」的斷言,都要回頭問一次「**B 裡有沒有 A 沒登記的**」。

## 誠實的邊界

1. **驗收者與實作者是同一個。**本輪未經審議席複審——`plugins/bizdoc` 與 `plugins/techdoc` 的孤兒事實是**使用者**在提問時直接指出的,我做的是查證與執行,不是發現。
2. **「刪除而非納管」的依據是 repo 自己寫下的判準**(兩份 SKILL.md 的引擎自述、zh-style 作為第三支引擎沒有 plugin 目錄的先例、KN-003 的觸發面判準),不是我另立的標準。若日後決定要把引擎也上架,推翻的是 KN-003,不是本張。
3. **`catalog_check --since` 在 commit 之前驗不到本張。**它比對的是 `origin/main..HEAD` 的 git diff,工作區的未提交變動不在範圍內——所以「刪檔要 bump」這一項,是先主動 bump、再於 commit 後複驗,不是被閘逼出來的。
4. **新斷言擋不住「登記了但內容是空的」。**三向相等只查名字,不查目錄裡有什麼。一個空的 `plugins/foo/` 只要同時寫進 `PLUGINS` 與 marketplace 就會過——那一層由 `catalog_check` 的 plugin.json 存在性接手,但兩者之間仍有縫。
5. **本分支與 `claude/fixture-cross-review` 的編號刻意錯開。**該分支已用掉 CHG/ACC-20260814-01,本張取 -02 以免合併時撞號;兩支分支尚未互相合併。

## 結論

**通過。** 9 條查核全數通過。刪除的 23 個檔在 git 歷史裡完整可還原,而新斷言讓同一種孤兒下次在 CI 直接轉紅——先對真實輸入證明紅燈可達,才動手刪。
