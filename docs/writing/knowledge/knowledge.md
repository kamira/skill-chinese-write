# knowledge — writing

> 規範見 `skills/ai-sdlc-autopilot/references/knowledge.zh-tw.md`。進場讀 INDEX、只載入 scope 內條目。

## INDEX(讀這裡,不是整份檔)

| id | tier | tags/scope | 一句規則 | 狀態 |
|----|------|-----------|----------|------|
| KN-001 | shallow | style · lint · **治理** | 寫下的每一條規則都要在同一輪配上斷言;配不上的要明講「這條靠人判斷」。五個層次都要問:規則有沒有斷言、斷言有沒有夾具會踩到、斷言的範圍等不等於規則的範圍、**斷言的方向是不是只有單向**、**夾具本身對不對**。「靠人判斷」的豁免只給**本來就判不了**的,不給**判得了但沒去判**的——後者叫「已知限制」,是第三種空頭規則的偽裝 | 生效(seen 10 / applied 10) |
| KN-002 | shallow | lint · 誤報 | 風格 lint 的門檻要按**文本類型**分流:條列、標題、引號內的文字都不該用散文的標準去量,否則規則越細誤報越多,使用者會學會忽略警告 | 生效(seen 3 / applied 3) |
| KN-003 | shallow | skill 邊界 · 拆分 | 三層判準:**拆不拆看觸發面**、**規則檔共不共用看硬規則是否互斥**、**規則不隨文體改變就放共用引擎**(zh-style) | 生效(seen 4 / applied 4,**2026-08-10 兩度改寫**) |
| KN-004 | shallow | 驗證 · 治理 | **驗證與寫入必須錨定同一棵樹,且每個數字都要當場量**——回報任何「閘綠」之前,先證明被驗物在被驗的樹裡(證據須含被驗檔絕對路徑與 `git rev-parse --show-toplevel` 輸出)。死 worktree 解析、相對路徑跨樹寫檔,都會產生「綠燈是真的、照的不是你那棵樹」的假驗收 | 生效(2026-08-14 起) |
| DIR-002 | directive | global · 治理 · 審議 | 需求確認、驗證、依 skill 產出的文章,以及**所有修正項目(凡開 CHG 者)**,一律交由 **codex + fable 互審並達成共識**;顆粒度錨定在**處置**,不是整張票;三輪不收斂即停、記帳、**主動**問使用者;一方不可用時可單方做可回復的候選施工,但不得記為共識、不得 merge,否決或僵局須回復至基準;斷言在 `chg_field_check.py` 判定三,自 `CHG-20260821-01` 前瞻生效 | 生效(2026-08-13 起,2026-08-21 擴大) |
| DIR-001 | directive | global · 治理 · 授權 | 本 repo 的**低/中風險**變更:自動 commit、開 PR、merge,不逐次問。高風險與永久停點不在授權內 | 生效(2026-08-10 起) |

## KN-001 — 風格規則要嘛有斷言,要嘛明講靠人判斷,不能有空頭規則

- tier:shallow
- 日期 / 分支:2026-07-28(UTC+0)/ claude/writing-skill-article-review-92efbc
- tags/scope:style · lint
- 觸發證據:
  1. **CHG-20260727-01**(v1.0.0 建檔):援引 `docs/ai-sdlc/knowledge` KN-001「治理規則必須有模板欄位 + lint 檢查對應」,把可判定的風格要求全部落成 `style_rules.json` 欄位與腳本斷言。
  2. **CHG-20260728-01**(v1.1.0):對照三支外部工具時發現,v1.0.0 的 `voice.md` 寫了「段落長度也要參差」,但 `style_check.py` 只檢查句長,**沒有任何段落層級的檢查**——同一份文件裡,句長那條有斷言、段落那條沒有,而我在同一輪還引用 KN-001 批評別人的空頭規則。自己犯規。
  3. **CHG-20260728-02**(v1.2.0):`voice.md` 對「字數對稱」有斷言,對同一類的**明喻**與**段末斷語**沒有——外部偵測器逐句判定指出被判最像 AI 的正是後兩者。同一型樣第三度出現,且又是自家犯規。
  4. **CHG-20260729-01**(v1.3.0):使用者五則規格全數落成斷言或明文標示靠人,無空頭規則——本條已內化為改版時的預設動作。
  5. **CHG-20260810-05**:ai-sdlc 自 v1.21 起要求中/高風險 CHG 附設計圖,這條規則在本 repo **從來沒有任何機器在查**。三張中風險 CHG 都有圖,純粹因為寫的人記得畫。前四次都發生在 skill 的風格規則上,**這次發生在治理規則自己身上**——寫規則的那一層,和被規則管的那一層,犯的是同一個錯。另,ACC-20260810-02 記過一次往下一層的變體:規則有斷言、但**斷言沒有夾具會踩到**(章節字數那條)。
  6. **CHG-20260810-07**:CHANGELOG 明訂版本要在**三處**同步(SKILL.md / plugin.json / marketplace),而 `catalog_check.py` 只比對其中**兩處**。`writing` 因此在 SKILL.md 停在 1.3.0、另外兩處是 1.4.0,全綠地活過一輪 CI。這次的變體是**涵蓋範圍**:規則管三處、斷言只查兩處——不是沒有斷言,是斷言比規則窄。
  7. **CHG-20260810-09**:`writing` 的第六條硬規則寫著「結尾不准用一句短話作總結」,但斷言只抓**長度**(低於 12 字)。使用者指出的四個總結式結尾都超過 12 字,**全部安全通過**——規則寫的是「總結」,斷言抓的是「短」。同一輪還發現 `bizdoc` 的 `subject_pattern` 是 `[::]`(兩個相同的半形冒號),意思是**它一直拒絕每一份標點正確的公文**;那份夾具能過,純粹因為它的標點也是錯的——**錯的夾具遮住了壞掉的規則**。
  8. **CHG-20260810-10**:模板要求 close-out 回填 `Commit/PR:`,`doc_integrity_check` 只驗那一行**存在**,不驗它**有內容**——兩張 CHG 停在「<close-out 回填>」一路綠燈進 main。同一輪還發現反向的一種:**對的夾具會逼出壞掉的規則**。成語下限那條錯了本來沒有代價(沒有輸入跑到它),樣稿一變成 CI 夾具,它就在每次 CI 對半數流派印反話。與第 7 次那個「錯的夾具遮住壞規則」方向相反,底層同一句:**規則的正確性由跑到它的輸入決定**。
  9. **CHG-20260814-02**:`plugins/bizdoc/` 與 `plugins/techdoc/` 被 git 追蹤,卻**不在任何一份名冊裡**——不在 marketplace、不在 `build_suite.py` 的 `PLUGINS`。三道既有的閘全部是**名冊 → 磁碟**(catalog_check 走訪 marketplace 條目、build_suite 與 skill_inventory 第 2 項走訪 `PLUGINS`),所以**不在名冊裡的東西,一道都走不到**。兩個目錄因此停在建檔當天,與單一真相分岔(對 46331e8 實測 11 檔、對 268d404 實測 13 檔——**缺口會隨時間擴大**,因為單一真相一直在動而孤兒不動;其中一份夾具用半形標點,是 zh-style 判為硬性違規的那一類),而 CI 全綠。這次的變體是**方向**:斷言只有單向,反向的那一半沒有人在查。與第 6 次的「涵蓋範圍」同族,差別在那次是查得不夠寬,這次是**只查得到被登記的東西,查不到沒被登記的**。
  10. **CHG-20260814-02(同一張,第二次)**:上一條補的第 6 項,使用者故事寫的是「`plugins/` 底下**不存在任何**沒被名冊登記的目錄,**以便『被 git 追蹤』等於『被治理閘掃得到』**」,而斷言只查 `plugins/` 的**第一層目錄名**。審議席用兩個實驗證明後半句是假的:`plugins/official/rogue-dir/` 與 `plugins/official/skills/rogue/` 都能被 git 追蹤、都不被 build_suite 同步,而兩道閘都 exit 0。**一張專門在修「斷言範圍窄於規則範圍」的變更,自己交出一個範圍窄於使用者故事的斷言。**主 agent 當時打算把它記進 ACC 的「誠實的邊界」放行,審議席判不通過:誠實標示的豁免權只給**本來就判不了**的東西,不給**判得了但沒去判**的東西——「已知限制」是第三種空頭規則的新偽裝。第 7 項(樹形登記)因此在同一張補上。
- 計數:seen 10 / applied 10 / last-applied 2026-08-14
- 狀態:生效(十度觸發,九次為自家犯規;**第 10 次發生在修第 9 次的同一張變更裡**,且不是主 agent 找到的)

### 規則

寫下一條可判定的要求時(**風格規則與治理規則同等適用**),同一輪必須做到其中一件:

1. 在 `assets/style_rules.json` 加對應欄位,並在 `scripts/style_check.py` 加斷言;或
2. 在該處明講「這條沒辦法機器判定,靠自己讀」——像 SKILL.md 的五維自評那樣,給結構但不假裝是斷言。

**不允許第三種**:寫成規則的樣子、放在規則文件裡、但沒有人會執行。這種規則比沒有更糟,因為它讓文件看起來完整。

**第二條路不是逃生門。**「明講靠人判斷」的豁免權,只給**本來就判不了**的東西——像 KN-003 那些沒有 lint 的 skill,或「有沒有一個別人沒講過的角度」。**判得了、只是我沒去判**的,寫進「已知限制」不算誠實標示,那是第三種的新偽裝(CHG-20260814-02 第二次觸發時被審議席擋下)。分界很簡單:如果補上斷言的工是可估的,那就補,不要記。

### 為什麼是這條而不是「規則都要能機器檢查」

不是每條都能機器化。「有沒有一個別人沒講過的角度」永遠檢查不了。分界不在「能不能檢查」,而在**有沒有誠實標示**——讀者(和模型)要能一眼看出哪些是硬線、哪些要自己判斷。

### 檢查方式

改完 references 之後,把新增的每一條規則列出來,逐條問:它在 JSON 裡有欄位嗎?沒有的話,文件裡有沒有講明它靠人?兩個都沒有就退回去補。

**再往下問一層**:那條斷言有沒有夾具會踩到?有規則、有斷言、沒有任何夾具會讓它響——文件一樣看起來完整,一樣沒有人在把關(ACC-20260810-02 的章節字數就是這樣被抓到的)。

**再往下問一層**:那份夾具本身對嗎?`bizdoc` 的主旨判定寫成 `[::]`(兩個相同的半形冒號),它一直拒絕每一份標點正確的公文——而夾具能過,是因為夾具的標點也是錯的。**錯的夾具會遮住壞掉的規則**,而且遮得很乾淨:CI 全綠。

**再往下問一層**:那條斷言查的範圍,等於規則管的範圍嗎?規則寫「三處同步」而斷言只比對兩處,和沒有斷言一樣會讓分岔活下來(CHG-20260810-07 就是這樣被抓到的)。

**再往下問一層**:那條斷言是**單向**的嗎?從名冊走訪磁碟的閘,永遠看不到**沒被登記在名冊上**的東西。`plugins/bizdoc/` 與 `plugins/techdoc/` 就是這樣在 main 上活著:被 git 追蹤、與單一真相分岔、三道閘一道都走不到、CI 全綠(CHG-20260814-02)。凡是「A 的每一項都要在 B 裡」的斷言,都要回頭問一次「B 裡有沒有 A 沒登記的」。

**治理規則也要問同樣五句話。**CHG 模板裡的每個欄位、每條「必須」,都要問它有沒有對應的閘、閘的範圍夠不夠寬。

## KN-002 — lint 的門檻要按文本類型分流,不能一套標準打全篇

- tier:shallow
- 日期 / 分支:2026-07-29(UTC+0)/ claude/writing-skill-article-review-92efbc
- tags/scope:lint · 誤報
- 觸發證據:
  1. **CHG-20260728-01**:外部來源把「先 A 再 B」「真正⋯的是」列高風險,但在繁中評論裡是自然口語,硬禁會天天誤報 → 改列軟限密度。
  2. **CHG-20260728-02**:明喻偵測初版把「不像」「很像」「讀起來像機器」這類平常比較一併算入(單篇誤報 4 處)→ 收緊 regex。
  3. **CHG-20260729-01**:條列一加進來就連續誤報三次(粗體上限、平板連段 22/23/22、對稱句「逐句標色/黃是機器」);標題「現象」被當成 2 字的句子扭曲節奏統計;引號內的「它像戒菸貼片」被算成明喻。
- 計數:seen 3 / applied 3 / last-applied 2026-07-29
- 狀態:生效

### 規則

新增或調整任何 lint 判定時,先問這條規則**該套用在哪一種文本**,再問門檻是多少。至少要分開四類:

1. **散文** —— 節奏、對稱、刻意句、長句上限的正常適用範圍
2. **條列** —— 本來就短而齊,句長與對稱一律豁免;自有規則(單項字數上限)
3. **標題** —— 不是句子,不進句長統計;自有規則(不能是結構標籤)
4. **引號內** —— use/mention:作者在**討論**那個詞,不是在用它;修辭類判定一律排除

**禁用詞與密度類是例外,它們全文適用**——引號內的例外走 `--allow` 個案放行,不走自動豁免。分流時要記得補這一刀:CHG-20260729-01 把條列與標題退出句子統計後,禁用詞一度跟著漏抓,是分流引入的回歸。

### 為什麼

誤報的代價高於漏報。lint 是給人看的,一旦它開始對正確的寫法報警,使用者學到的是「忽略警告」,那整套規則就作廢了。分流做得對,規則可以越加越細;分流沒做,規則越細越吵。

### 檢查方式

加一條新判定之後,拿三份東西各跑一次:一篇純散文、一篇有條列與小標的議題型、一篇引用大量原文的稿子。任何一份出現不該有的警告,先修分流再談門檻。

## KN-003 — 拆不拆看觸發面,共不共用看硬規則(2026-08-10 改寫)

- tier:shallow
- 日期 / 分支:2026-08-10(UTC+0)/ claude/techdoc-skill,同日於 claude/genre-split **改寫**
- tags/scope:skill 邊界 · 拆分
- 觸發證據:
  1. **CHG-20260810-03**(新增 `fiction`):拆的理由不是「小說跟評論是兩種文體」,而是 `writing` 的三條硬規則對小說**全是反的**。共用一支 lint 會直接誤殺。
  2. **CHG-20260810-04**(新增 `techdoc`):反過來的情況。規格書與架構說明的硬規則**完全相同**,只差結構;拆開會產生兩份 90% 重複的規則檔,而重複的規則檔一定會分岔。
  3. **CHG-20260810-08**(23 支前門):使用者指出前兩版判準**少算了觸發面**——有人說「幫我寫一篇武俠」時,應該打中一支叫武俠的 skill,而不是先知道要載入 `fiction` 再加 `--genre wuxia`。**skill 的第一個功能是被找到**,而原判準只算了工程成本(規則檔會不會分岔),完全沒討論這一層。
- 計數:seen 4 / applied 4 / last-applied 2026-08-10
- 狀態:生效(第二次改寫,加上第三層)

### 規則:兩件事分開問

原判準把兩個不同的問題混成一個,所以會得出「規格薄的先不拆」這種只從成本面看的結論。拆成兩層之後兩邊都成立:

| 問題 | 判準 | 錯了會怎樣 |
|------|------|-----------|
| **要不要獨立成一支 skill?** | **觸發面**:使用者會不會用這個名字來找它 | 找不到,等於沒做 |
| **規則檔與腳本共不共用?** | **硬規則互不互斥**:相反就各自一份,相同就共用 | 共用會誤殺,分家會分岔 |

答案的四種組合:

1. **獨立 + 自己的引擎**——硬規則與現有的都互斥(`writing` / `fiction` / `techdoc` / `bizdoc`)
2. **獨立 + 指回引擎**——會被單獨尋找,但硬規則與某支引擎相同(`fiction-wuxia`、`spec`、`official`)。plugin 打包時**必須把引擎帶上**,否則裝了跑不動
3. **獨立 + 沒有引擎**——會被單獨尋找,但沒有任何可判定的硬規則(`prose`、`poetry`)。SKILL.md **必須明標「本支沒有 lint,規則靠人判斷」**——這是 [[KN-001]] 的第二條路,不是空頭規則
4. **不獨立**——不會有人用這個名字找它。留在 `docs/genres.md`

### 為什麼原本那條錯了

原本的第三條寫「規格薄到落不成斷言的先不拆」。那句話把「沒有 lint」當成「不該存在」——但 KN-001 從一開始就允許第二條路:**明講靠人判斷**。一支誠實標示自己沒有斷言的寫作指引,是合法的 skill;真正被禁的是**假裝有斷言**。

### 檢查方式

先問「使用者會用什麼字來找它」。會的話就建前門,接著才問「它的硬規則和現有引擎相同還是相反」——相同就指過去並在 plugin 打包時帶上引擎,相反就新建引擎,兩者皆非(沒有硬規則)就明標沒有 lint。

`scripts/skill_inventory_check.py` 把後兩件事變成斷言:沒被任何 plugin 打包的 skill 會紅、前門叫人跑引擎卻沒打包引擎會紅、沒有 lint 又缺明標節會紅。

### 觀察中

23 支之後,規則檔仍然只有四份(引擎)。前門與引擎分層是為了避免分岔,但代價是**前門的內容沒有任何斷言在管**——SKILL.md 寫錯了流派配比不會有人發現。第二次出現前門內容與引擎規則檔不一致時,要考慮把配比也移進規則檔。

## KN-004 — 驗證與寫入必須錨定同一棵樹

- tier:shallow
- 日期 / 分支:2026-08-14(UTC+0)/ claude/skill-consolidation
- tags/scope:驗證 · 治理
- 觸發證據:
  1. **CHG-20260814-03**:施工時把新開的 CHG 寫進一個**已失效的 worktree 目錄**
     (該目錄只剩 `docs/`,已不在 `git worktree list` 裡),不在 repo 內。
     隨即回報「三道閘全過」——**那是假的**:`chg_diagram_gate`、`chg_field_check`、
     `doc_integrity_check` 掃的是 repo 內的 20 份,**根本沒看到那一份**。
     成因:`git rev-parse --show-toplevel` 從死 worktree 目錄解析會**落回主 repo**,
     而寫檔用的是相對路徑,於是**驗證的樹與寫入的樹不是同一棵**。
     發現後移進 repo(21 份)重跑才是真過。
  2. **CHG-20260814-04**:同一輪的 ACC 把 E-6 的觸及範圍寫成「五檔」,實際 7 檔。
     成因與第 1 次同源——**用回憶代替量測**:寫 E-6 時拿的是寫 ACC 之前的
     `git status --short`(三個檔),心裡加上記得的兩個湊成五,沒有真的跑 `git diff`。
     位置更難堪:同一份 ACC 的「審議席抓到而我沒抓到的」第 4 條,記的正是
     「我漏報觸及範圍——回報四檔,實為五檔」。**在承認漏報的那一頁上又漏報一次。**
     同輪還有一件:**merge 按在審議席裁決之前**,而裁決把該錯誤列為阻擋 merge。
- 計數:seen 2 / applied 2 / last-applied 2026-08-14
- 狀態:生效

### 規則

**回報任何「閘綠」之前,先證明被驗物在被驗的樹裡;而且每個數字都要當場量,不能用回憶。**

留證至少要有三樣:

1. 被驗檔的**絕對路徑**
2. `git rev-parse --show-toplevel` 的輸出
3. **凡數量、檔數、案數,附產生它的那道指令與原始輸出**(第 2 次觸發後擴寫)

前兩者對不起來就不是驗過,是驗了別的東西;第三者缺席就是**用回憶代替量測**——
兩次事故的共同成因。ACC 的證據欄依此紀律填寫。

**附帶一條停點紀律**:一件事已經送審議席、裁決還沒回來時,
「沒問題」這個判斷**本身還沒有依據**。不可逆動作在那個窗口裡不得執行。

### 為什麼不是 KN-001 的又一個變體

KN-001 至今十次觸發,全部是「**斷言比規則窄 / 單向 / 夾具本身錯**」——
斷言照的**物件**從來沒錯過,錯的是照的**範圍**或**方向**。

這一次不同:**斷言全對、跑得全綠,但照的不是你改的那棵樹。**
這是驗證完整性的新失效軸,與規則本身的窄或方向無關。所以另立條目。

與 KN-001 的分工:
KN-001 問「這條規則有沒有斷言、斷言涵蓋得夠不夠、跑到它的輸入對不對」;
KN-004 問「**你剛才跑的那個斷言,看的是不是你剛才改的那個檔**」。

### 適用範圍

不限 worktree。任何「產出位置」與「驗證位置」可能分離的情況都適用——
暫存目錄、多 repo、容器內外掛載、以及本 repo 的 `plugins/` 建置產物
(單一真相在頂層、生成物在 plugin 內,驗錯一邊就是同一個病)。

## DIR-002 — 需求、驗證、文章與**所有修正項目**交由 codex + fable 共議達成共識

- tier:directive
- 日期 / 分支:2026-08-13(UTC+0)/ claude/fixture-cross-review;
  **2026-08-21 擴大**(UTC+0)/ claude/ai-sdlc-handshake-c7b2a2
- tags/scope:global · 治理 · 審議
- 觸發證據:
  1. **CHG-20260813-01**:使用者指定「往後驗證、需求確認都交由 codex 和 fable 相互審議」,
     其後追加「依 skill 產出的文章內容也要交叉審查」與「不再人審」(評分權一併移交)。
     成效可量:審議席在該張抓到的實質缺陷包括——套套邏輯的 self-test(集合與自己取交集,
     數學上不可能失敗)、FORMAT_PHRASES 的插入式後門、主 agent 在「修正」時改壞的三處小說夾具、
     ACC 自己的結案框架搶跑。**多數不是主 agent 找到的。**
  2. **CHG-20260814-01**:codex 的 MCP server 中途斷線,而當下正卡在兩席分歧上
     (「(3)(4) 兩條未做可否收尾」)。DIR-002 當時**沒有寫一方不可用時怎麼辦**,
     只好現場發明處置——這是它的第一個缺口,已補上。
  3. **CHG-20260821-01**:使用者把範圍擴大到「所有修正項目」、把強度提高到「達成共識」。
     同一張補上本條**一直沒有的斷言**——它生效八天,沒有任何機器在看它,
     全靠主 agent 記得送審(KN-001 第 10 次)。
- 計數:seen 3 / applied 3 / last-applied 2026-08-21
- 狀態:生效

### 規則

**1.(送審範圍、邊界、顆粒度)** 需求確認、驗證/驗收、依 skill 產出的文章,以及**所有修正項目**,
送 codex 與 fable 各自獨立審,再由主 agent 交叉比對。

**邊界錨定在 CHG**:凡需要開 CHG(含 lite)的變更——規則、斷言、閘、skill 內容、治理文件、
帳本條文的任何修改——都是修正項目,須依第 2 款達成共識。要不要開 CHG 有疑義時,**視為要**
——與 DIR-001「分不清就當高風險」同一個方向。

**顆粒度錨定在處置**:一張 CHG 內每個可獨立接受、拒絕、回復或驗收的實質處置,
都要列成唯一且穩定的**修正項目 ID**(格式:該張 CHG 編號後綴 `.1`、`.2`……),
逐項取得共識,ACC 逐項承接相同 ID。**只有一個處置時仍須列一個 ID。**
不得以同一張 CHG、同一目的或同一批施工為由,把可分別裁決的處置合併成一項。

切項由提案方(通常是主 agent)先切;提出與調整切項屬該張 CHG 的審議迭代,本身不另構成修正項目,
但兩席的同意必須**同時涵蓋項目邊界與各項處置**。任一席要求拆項,即構成分歧,走第 2 款收斂;
在該分歧收斂前,不得認定相關項目已有共識。**主 agent 不得以切項權替任何一席裁決。**
「票內是否還藏著未列出的獨立處置」機器判不了,依 KN-001 第二條路**明講靠人**,
列為審議席的審查職責。

以下三類不是修正項目:

- 同一張 CHG 的審議迭代內,落實審議席自己指出的修正;
- 握手、worklog、`## Status` 與 close-out 欄位的例行回填;
- 錯字與排版修正,且**不觸及** `skills/*/assets/`、`tests/fixtures/`、任何 `*_rules.json`
  與 `scripts/`——夾具與規則檔的「排版」就是判定對象,不在豁免內。

排除事項若改變規則、語意、驗收結果或證據解讀,仍須列項。

**2.(共識的操作定義、收斂上限、僵局出口)** 共識 = 兩席**各自明示**同意**同一個處置**;
沉默、未審、附條件同意、僅同意方向、重試逾限,都不是同意。

收斂以**三輪**為上限:第一輪各自獨立審,之後把分歧本身連同雙方理由送回兩席,至多再往返兩輪。
主 agent 僅得整理分歧、提出修訂案,**不得代為拍板**;修訂案須以相同版本送回兩席。
任一席改變判定時,須明示改判及理由。

三輪內達不成共識時:

- 該修正項目**不執行**,維持現狀——fail-closed,僵局不是放行;
- 已有未共識候選施工者,依第 3 款回復;
- 兩席已同意的子集可切出先行,爭點單獨掛起,不得以一個爭點凍結不相干的工作;
- 僵局連同雙方**最終立場與理由原文**記入帳本(觀察中或 BL 條目);
- 不可逆動作(merge / PR / 發布)一律等,未取得使用者裁決前維持不執行;
- **僵局不得靜默維持現狀**:記帳完成後,主 agent 必須在當輪回報中**主動**告知使用者
  該項已擱置、擱置原因、以及雙方的最終立場與理由原文,不得等使用者來問——
  靜默擱置會讓使用者以為指令已被執行。

**使用者是唯一的第三席**;主 agent、codex、fable 或其任意組合均不得代裁。

**3.(一方不可用時)** **缺席不停擺,但缺席期間沒有共識**:

- 單一可用席得裁決並執行**可完整回復**的候選動作(產文、實跑閘、施工、寫 ACC 並註明單方);
  單方寫入僅及 branch,不及 main;
- 施工前須記錄:缺席方、缺席原因、**施工前基準 revision**、修正項目 ID、完整變更檔案清單;
- 該施工標示為**未共識候選**,不得宣稱通過或完成;CHG 的 `- Consensus:` 欄位標
  `單方(<缺席方>缺席:<原因>)`;
- **不得** commit 進 main、開 PR、merge、發布,**亦不得夾帶進其他已通過項目**;
- 仍然**不可以**單方執行對造**已投反對票的那個動作**——在它缺席期間執行它反對的事,
  就算它錯,也是缺席逕付;
- 缺席方恢復後,首項議程是覆核缺席期間的單方裁決,**不因既有施工而推定同意**,覆核得整批推翻;
- **任一席否決,或三輪仍未形成共識時,候選施工須回復至記錄的基準**;
  僅保留審議、worklog、backlog 與僵局紀錄;
- **優先序**:可回復動作上,本款(單方繼續)優先於全面停擺;不可逆動作上,
  「共識齊才可 merge」優先於 DIR-001 的自動 merge 授權——DIR-001 不撤銷,
  但自本條生效日起多一個前置輸入,它只免除逐次詢問,不免除審議與共識;
- ACC 具名標示缺席方與原因,**不得記為互審通過**。

**4.(重試節奏)** API 過載時:一次一項、間隔 5~10 分、同項最多五次,逾限標「未審」續下一項。

**5.(斷言)** 分兩層,均 fail-closed,均自 `CHG-20260821-01` 起前瞻生效,
檔名編號在此之前的既有 CHG **一律豁免**——不追溯、不補造,
與 `chg_diagram_gate.py` 的 `DIAGRAM_SINCE` 同一手法。

**第一層(每張都要)**:CHG(含 lite)標頭新增 `- Consensus:` 欄位,
`scripts/chg_field_check.py` 增設判定三,掛在 `.github/ci_local.sh` 第 [9/19] 步。
該欄位必須存在、非佔位,且匹配下列**四種**形狀之一,否則紅:

- 兩席具名:`codex ✓ <日期> / fable ✓ <日期>`——必須**同時**含 codex 與 fable;
- 單方:`單方(<缺席方>缺席:<原因>)`——理由空白視同未宣告,紅;
- 僵局:`僵局(記於 <BL 編號>,待使用者)`;
- 使用者裁決:`使用者裁決(記於 <編號>)`。

**「單方」、「僵局」、「使用者裁決」都不是兩席共識**:它們合法出現在欄位裡,
但不得據以宣稱互審通過。

**第二層(逐項表)**:每張 CHG 都必須在 `## 修正項目` 列出**至少一個**修正項目 ID;
列了**兩個以上**時,必須另附 `## 審議席共識` 表,每項一列:

    修正項目 ID | 處置版本 ID | codex 判定 | fable 判定 | 共識狀態 | 收斂輪次 | 未決理由

處置版本 ID 唯一指向同一張 CHG 內一段完整、可讀、可區別版本的處置文字;
處置實質改變時必須建立新版本 ID,**不得覆寫舊版本後沿用原判定**。

**不設內容雜湊**——雜湊沒有穩定可當場量測的對象(審議期間檔案與 revision 都會變),
且會讓閘因與不變量無關的理由轉紅(`chg_field_check.py` 的 `STATUS_BASELINE` 註解已記過同型)。

閘對此表只查**引用與狀態的一致性**:每個已宣告 ID 恰有一列、每列七欄齊備、
`通過` 僅可出現在兩席判定皆為「同意」時、收斂輪次不得超過三。
**標頭與表不得互相說謊**:含逐項表的 CHG,標頭僅可在**每一列**共識狀態皆為 `通過` 時
使用兩席具名形狀——任一列不是通過而標頭具名兩席,即紅。

**判不了的四件事,依 KN-001 明講靠人判斷,不得偽裝成斷言**:

1. **欄位可以謊填,同版本 ID 下的處置文字也可以被覆寫**——閘查得了形狀與引用,
   查不了共識是真的,也查不了文字沒被改過;雜湊撤回後,「不得覆寫舊版本後沿用原判定」
   這條禁令沒有斷言,歸對造覆核:謊稱對方同意的欄位、或覆寫後沿用的判定,對方下一輪就會看到。
2. **票內是否還藏著未列出的獨立處置**——閘看不出一段散文裡藏著幾個處置;
   防線是審議席的審查職責。
3. **該開 CHG 而沒開的變更**——沒有檔可掃,閘全盲;
   防線是審議席審 branch diff 時比對「這批 diff 有沒有對應 CHG」。
4. **單項 CHG 的逐項表不強制**(使用者裁決的分階段安排,`BL-032`)——
   單項票的「兩席同意的是哪個處置版本」目前只有標頭一格,閘查不到那條鏈。

### 為什麼是 directive 而不是 KN

它規範的是**誰有權判定**,不是某類技術問題的處置方式。與 DIR-001 分工:
DIR-001 管「要不要停下來問人」,DIR-002 管「誰來判對錯」。
兩條相衝時見第 3 款的優先序——DIR-001 只免除逐次詢問,不免除審議與共識。

## DIR-001 — 低/中風險變更預先授權:直接 commit、開 PR、merge

- tier:directive(使用者明示指令)
- 日期 / 分支:2026-08-10(UTC+0)/ claude/ai-sdlc-autopilot-handshake-4a3f7a
- tags/scope:global · 治理 · 授權(**全分支適用**)
- 來源原話:「auto commit and create pr and merge, no ask if low or medium risk」
- 狀態:生效

### 規則(邊界寫窄)

**在 `kamira/skill-chinese-write` 這個 repo 內**(2026-08-10 前名為 `kamira/skill-write`),變更風險評為**低或中**時,以下四個閘不再逐次徵詢,直接執行:

1. commit(訊息帶 CHG 編號)
2. push 並開 PR
3. 等 CI 完成
4. CI 全綠後 merge

CI 未完成、非綠、或**判定不出狀態**時,一律停——merge 是單向門,這道閘 fail-closed,不在授權範圍內。

### 不在授權內(仍要停下來問)

- **高風險**變更:資料模型/遷移、認證授權、金流、刪除或不可逆、跨模組介面、安全邊界。命中清單即為高風險,不接受自評降級。
- **永久停點**:不可逆刪除、付款、正式環境資料遷移、安全邊界變更。任何設定都關不掉。
- 風險等級**判定不出來**的時候。分不清就當高風險。

### 為什麼記在這裡而不是只記在 CHG 裡

預先授權是跨變更、跨 session 生效的,寫進單張 CHG 的話下一個 session 讀不到——而讀不到的授權等於沒有授權,下一棒還是會逐次問。

### 撤銷條件

依 CHG-lite 的誤射規則:本授權涵蓋的某一類變更事後被抓到弄壞東西(迴歸或驗收失敗),**本條自動失效**,退回逐次徵詢,直到使用者重新授權。

## 觀察中(未達建檔門檻,計數用)

| 型樣 | seen | 來源 | 備註 |
|------|------|------|------|
| **BL-004** bizdoc 成語密度沒有任何夾具跑得到 | 1 | CHG-20260813-01 | `[10d]` 釘死的三份未驗到裡,`bizdoc/sample-gov-good.md` 讓成語密度規則**沒有任何輸入到得了它**——這正是 D-1 要修的 KN-001 狀態被 min_sample_chars 重新引入。V5#4 修的是「靜默」,缺口本身還在。**完成標準**:bizdoc 有一份 ≥300 字、會實際踩到成語密度判定的夾具(紅端或綠端皆可),且該份從 `[10d]` 的未驗到名單移出 |
| **BL-001** 每條提醒規則要有一組受 `--strict` 約束的正反例 | 1 | CHG-20260814-01 | 審議席(codex)第 (3) 條。目前只有成語密度做到,擬聲詞密度、章節字數、軟限詞等仍無。**完成標準**:規則清單 × 正反例對照表,無一漏項 |
| **BL-002** `per_k` 收成回傳「未驗到 \| 密度值」的 helper + 契約測試 | 1 | CHG-20260814-01 | 審議席(codex)第 (4) 條。現況是散落守衛 + `[10c]` 端到端防線;端到端保護不了未來新增的呼叫端。**完成標準**:呼叫端不得直接除 `per_k`,helper 層級契約測試涵蓋 0 / 低於門檻 / 剛好門檻 / 一般文本 |
| **BL-003** G-1 同義表述與 G-2 近似比對的評估案 | 1 | CHG-20260814-01 | 審議席(codex)第三題:10 字精確比對可被改寫繞過、meta 黑名單漏同義表述。**完成標準**:必含誤殺成本分析與對全夾具的誤報實測,兩件沒做完不准上線——本分支已因過度一般化被打回兩次 |
| hard 禁用詞需要 `--allow` 逃生口 | 1 | CHG-20260727-01 | 一刀切禁詞在引用法條或他人原話時會誤殺。2026-07-28 經使用者確認**保留**此設計;若其他 lint 也冒出同樣需求就建 KN |
| 外部偵測器的判準可能與本 skill 的價值觀相衝 | 1 | CHG-20260728-02 | GPTZero 獎勵修辭與文學性、懲罰清楚與精確,與本 skill 主張相反。處置:寫成「已知代價」,不轉成規則。第 2 次遇到別的偵測器/評分器有同樣衝突即建 KN |
| 閘失敗時,自己的正常輸出蓋掉失敗原因 | 1 | CHG-20260810-07 | pre-push hook 的九步輸出把最後那行失敗原因擠出畫面,導致我改用 `--no-verify` 去看訊息——**繞過閘來讀閘的錯誤**。處置:失敗時把原因重印在最後一行。第 2 次發生即建 KN |
| 使用者規格會連續到達,不要改到一半就發版 | 1 | CHG-20260729-01 | 本輪五則規格分五次給出,若每則各開一個 CHG 會產生五次版本 bump 與五次 PR。處置:累積後一次收。第 2 次出現即建 KN |
