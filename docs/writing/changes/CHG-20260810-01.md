# CHG-20260810-01 — 對齊過期的 live handshake 檔,並記下使用者的預先授權

- Project: skill-write / skill: writing
- Date: 2026-08-10 (UTC+0) | Branch: claude/ai-sdlc-autopilot-handshake-4a3f7a | Risk: low | Implemented by: Claude(Cowork session)
- Template: lite
- Skill: ai-sdlc v1.22
- Type: 文件(worklog + knowledge)
- Proposed by: 使用者(「把 handshake 檔對齊」→「auto commit and create pr and merge, no ask if low or medium risk」)
- Motivation: `docs/worklog/handshake-autopilot.md` 停在 CHG-20260804-02「待收尾」,但該張與 CHG-20260804-03 都已驗收——交接檔宣稱有東西在飛,而實際上沒有;同輪把使用者剛給的預先授權記成 DIR-001。
- Commit/PR: <close-out 回填>
- Recurrence check: 非重複。本 repo 首次出現 live handshake 過期,亦為首則預先授權指令。
- Related: docs/writing/changes/CHG-20260804-03.md(前一張,交接檔停在它的前一張)

## 這一輪改了什麼

| 檔案 | 改法 |
|------|------|
| `docs/worklog/handshake-autopilot.md` | 機器區塊由 `CHG-20260804-02 / 待收尾` 對齊為 `CHG-20260804-03 / 無在飛 task`;新增人手交接節於 `<!-- autopilot:begin -->` **之前**,使 runner 的 `handshake_block()` 覆寫機器區塊時不會動到它 |
| `docs/writing/knowledge/knowledge.md` | 新增 DIR-001(預先授權:低/中風險自動 commit + PR + merge,不逐次問),INDEX 同步 |

受治理內容(skills / plugins / tools / structure / 既有 CHG 與 ACC)零改動。

## 為什麼列低風險

命中 CHG-lite 白名單的「docs-only」:不碰結構、資料、介面,不改任何會被執行的程式碼。DIR-001 是把使用者剛講的話逐字記下,不是我替他做的決定。

## 風險與回滾

- 風險:DIR-001 放寬的是「中風險 merge」這個閘,而政策表原本要求停下。這是使用者明示的放寬,邊界寫窄(見 DIR-001),永久停點不受影響。
- 回滾:`git revert` 本次 commit;交接檔與 knowledge 各自還原一節。

## Status

Accepted — self-verified (low risk):
`python3 tools/autopilot/scripts/doc_integrity_check.py --repo .` exit 0、
`python3 tools/tools_drift_check.py` exit 0、
`bash .github/verify.sh` exit 0(見下方 PR 的 CI 全綠)
