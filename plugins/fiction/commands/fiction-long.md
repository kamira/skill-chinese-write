---
description: 寫中／長篇小說(繁中)——長篇、連載、多線劇情、宏大世界觀。套用 fiction 主層規範 + 長篇配比。
---

# 中／長篇小說

使用者要寫的題目/需求:$ARGUMENTS

先確認篇幅與載體(實體書 or 網路連載),再依下面的規範動筆。這是 **`fiction` 主層的子層**:
主層管小說怎麼被讀進去(對話、分段、分章),本支管這個流派怎麼寫。

## 篇幅

數萬至數十萬字

## 怎麼寫

時空跨度大,**多線索主副線交織**,人物眾多,世界觀宏大。

多用**烘托**(用環境替人物的情緒說話)、**層遞**、**排比**(大場面時)。長篇的難處不在單章寫得好,在於第三十章還記得第三章埋的東西。

## 兩個配比,以及各自誰在管

**修辭比例:中高 25%-35%(風格目標,靠人判斷)。** 沒有程式量得出一段文字有幾成是譬喻,
這個百分比沒有機器真相,不會有任何 lint 驗它。

**成語密度:建議 4-8 次/千字(風格目標,靠人判斷)。**
lint(`--genre long`)**只擋上限 8**;**下限為 0、引擎不查**——原始規格沒給下限,
見 `fiction_rules.json` 的 `floor_removed_reason`。寫 0 個成語一樣過閘。

上面的分寫是刻意的。原本一句話寫成「X-Y 次/千字|lint 會數」,讀起來像上下限都在管;
實際上下限是 `CHG-20260810-10` 刻意拆掉的——引擎裡連那段程式都不存在,
`lo, hi = band` 的 `lo` 解出來就丟掉。**宣稱引擎在管、實際沒有,比沒有規則更危險**,
因為你會以為有東西在替你把關。

凡一個區間裡機器只管一端,就不准用單一句「lint 會數」帶過。上限那個 8 也是本 skill
自訂的區間,不是原始規格的百分比。

## 成語落在哪裡

跨越宏大時空時,用來做快速的「時間跳躍」或「全景式速寫」,避免拖沓。

典型:「寒來暑往,一晃十年」——八個字換掉一整段流水帳。

**落腳段落**:【章節開頭的大環境交代】與【過渡性的時間跳躍段】

## 主層的硬規範一樣要遵守

換人說話就獨立成段(同段最多兩處引號)、段落字數上限(實體 150 字 / 網文 80 字且不超過 3 行)、擬聲詞不用「X!」一聲的樣板、章節字數區間。細節見 [`references/dialogue.md`](../skills/fiction/references/dialogue.md) 與 [`references/structure.md`](../skills/fiction/references/structure.md)。

## 這個流派靠你自己判斷的

- **伏筆有沒有收**。程式數得出章數,數不出你埋了幾條線、收了幾條。
- **主副線的比重**。副線壓過主線是長篇最常見的失控,而它沒有任何量化特徵。
- **切章點**(斷頭台法則)。lint 量得出章有多長,量不出你停在哪一秒。

## 不要拿 writing 的評論規則套小說

那支要求第一人稱、禁短句收尾、限制刻意句——三條對小說都是反的。

## 交稿前跑 lint

lint 在主層,本支不另外寫一份——武俠與科幻的規則檔會有 95% 相同,而重複的規則檔一定會分岔。安裝本 plugin 時主層會一起帶進來。

```
python3 skills/fiction/scripts/fiction_check.py 稿件.md --genre long
```

網路連載加 `--mode web`(段落上限收緊為 80 字且不超過 3 行)。
